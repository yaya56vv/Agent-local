class RagClient:
    def __init__(self, base_url: str):
        """Initialise le client MCP pour le service RAG."""
        self.base_url = base_url

    # TODO: implémenter add_document(self, content, metadata, dataset),
    # query_rag(self, question, dataset, top_k), list_documents(self, dataset),
    # delete_document(self, doc_id), list_datasets(self)
