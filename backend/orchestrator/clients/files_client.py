class FilesClient:
    def __init__(self, base_url: str):
        """Initialise le client MCP pour le service de fichiers."""
        self.base_url = base_url

    # TODO: implémenter read_file(self, path), write_file(self, path, content),
    # list_dir(self, path), mkdir(self, path), delete_file(self, path),
    # file_search(self, pattern, path), get_file_info(self, path)
