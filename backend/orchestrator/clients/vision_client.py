class VisionClient:
    def __init__(self, base_url: str):
        """Initialise le client MCP pour le service de vision."""
        self.base_url = base_url

    # TODO: implémenter analyze_image(self, image_source, prompt),
    # describe_image(self, image_source), extract_text(self, image_source)
