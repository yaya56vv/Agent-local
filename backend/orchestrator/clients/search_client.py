class SearchClient:
    def __init__(self, base_url: str):
        """Initialise le client MCP pour le service de recherche."""
        self.base_url = base_url

    # TODO: implémenter web_search(self, query, max_results),
    # get_page_content(self, url), search_news(self, query)
