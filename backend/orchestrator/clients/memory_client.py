class MemoryClient:
    def __init__(self, base_url: str):
        """Initialise le client MCP pour le service de mémoire."""
        self.base_url = base_url

    # TODO: implémenter store_memory(self, session_id, role, content, metadata),
    # retrieve_history(self, session_id, limit), search_memory(self, query, session_id, limit),
    # clear_session(self, session_id), get_stats(self)
