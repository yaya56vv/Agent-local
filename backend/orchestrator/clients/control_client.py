class ControlClient:
    def __init__(self, base_url: str):
        """Initialise le client MCP pour le service de contrôle."""
        self.base_url = base_url

    # TODO: implémenter move_mouse(self, x, y), click_mouse(self, button, clicks),
    # type_text(self, text), press_key(self, key), take_screenshot(self),
    # get_mouse_position(self)
