class SystemClient:
    def __init__(self, base_url: str):
        """Initialise le client MCP pour le service système."""
        self.base_url = base_url

    # TODO: implémenter get_system_info(self), list_processes(self),
    # execute_command(self, command), get_clipboard(self), set_clipboard(self, text)
